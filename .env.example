# Database (PostgreSQL + pgvector extension harus aktif)
DATABASE_URL=postgresql://user:password@localhost:5432/diajar_db

# NextAuth
AUTH_SECRET=generate-dengan-openssl-rand-base64-32
AUTH_URL=http://localhost:3000

# Anthropic (untuk AI Q&A — generate jawaban dari hasil retrieval)
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Voyage AI (untuk embedding transcript — Anthropic tidak punya embedding model sendiri)
VOYAGE_API_KEY=xxxxx

# YouTube Data API (untuk ambil metadata & transcript video)
YOUTUBE_API_KEY=xxxxx
